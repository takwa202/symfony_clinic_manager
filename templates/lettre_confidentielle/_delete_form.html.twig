<form method="post" action="{{ path('app_lettre_confidentielle_delete', {'idconf': lettre_confidentielle.idconf}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lettre_confidentielle.idconf) }}">
    <button class="btn">Supprimer</button>
</form>
