{% extends 'acceilmed.html.twig' %}

{% block title %}Bilan{% endblock %}

{% block body %}
    <div style="background-color : #FFC0CB"> <h1>Bilan</h1> </div>


    <table class="table">
        <tbody>

            <tr>
                <th>Type</th>
                <td>{{ bilan.type }}</td>
            </tr>
            <tr>
                <th>DateBilan</th>
                <td>{{ bilan.dateBilan ? bilan.dateBilan|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Conclusion</th>
                <td>{{ bilan.conclusion }}</td>
            </tr>
        </tbody>
    </table>


    <button type="button" class="btn btn-light"><a href="{{ path('app_bilan_index') }}">retour à la liste d'affichage</a></button>
    <button type="button" class="btn btn-light"><a href="{{ path('app_bilan_edit', {'idBilan': bilan.idBilan}) }}">modifier</a></button>


    {{ include('bilan/_delete_form.html.twig') }}
{% endblock %}
