<form method="post" action="{{ path('app_pharmacien_delete', {'idPharmacien': pharmacien.idPharmacien}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharmacien.idPharmacien) }}">
    <button class="btn">Delete</button>
</form>
