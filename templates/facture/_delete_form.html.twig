<form method="post" action="{{ path('app_facture_delete', {'idfact': facture.idfact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.idfact) }}">
    <button class="btn">Delete</button>
</form>
