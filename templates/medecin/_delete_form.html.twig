<form method="post" action="{{ path('app_medecin_delete', {'idMed': medecin.idMed}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medecin.idMed) }}">
    <button class="btn">Delete</button>
</form>
